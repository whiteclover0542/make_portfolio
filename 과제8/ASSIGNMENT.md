# 과제 8 — 내 소개 페이지에 패스키 달기

> 이 문서는 과제 지침·요구사항을 그대로 옮겨 정리한 것입니다. 이후 수정하지 않습니다.
> 진행 상황·체크리스트는 [progress.md](progress.md)에 기록합니다.

## 개요

- **제목**: 내 소개 페이지에 패스키 달기 — 비밀번호 없이 나만 들어가기
- **계획 시간**: 6~7h
- **핵심 요구사항**: 1번 과제(소개 페이지)에 나만 볼 수 있는 자리를 하나 만들고, 그 자리를 패스키로 잠근다. 비밀번호는 만들지 않는다.
  - 등록: 내 기기가 열쇠 한 쌍(공개키·개인키)을 만들고, 공개키만 서버로 전송. 개인키는 기기 밖으로 나가지 않는다.
  - 로그인: 서버가 매번 새 질문(challenge)을 보내고, 기기가 그 질문에 서명해 답한다.
  - 기기 분실 대비: 패스키를 두 개 등록해 두고, 하나를 지운 뒤에도 들어갈 수 있는지 확인한다.
  - 공개 소개 내용은 지금처럼 누구나 볼 수 있게 둔다. 잠그는 것은 새로 만든 나만의 자리뿐.
  - 실제 연락처·신분증 번호 같은 진짜 개인정보는 넣지 않는다. 만들어 넣은(더미) 내용으로 충분하다.

## 왜 이걸 하는가

비밀번호를 아예 쓰지 않고 잠그는 방법을 직접 붙여 보는 과제. 7번에서 비밀번호를 어떻게 지키는지 배웠다면, 8번에서는 비밀번호를 없애면 무엇이 대신 그 자리를 채우는지 본다. 공개키와 일회용 질문, 그리고 기기를 잃어버렸을 때의 대비까지가 한 묶음이다.

## 제출물

1. **공개 결과물 주소** — 첫 화면은 누구나 볼 수 있는 소개 페이지이고, 비공개 자리만 패스키로 잠겨 있음
2. **소스 주소** — 등록·로그인·로그아웃·비공개 자료 조회가 지나는 자리를 찾을 수 있게
3. **인증 구현 설명서 여섯 항목**
   1. 무엇으로 붙였나 (직접 구현 / 라이브러리 / 인증 서비스, 이름)
   2. 왜 그걸 골랐나
   3. 어디를 어떻게 고쳤나 (등록·로그인·로그아웃·비공개 자료 조회 네 흐름이 소스의 어디를 지나는지)
   4. 안 열리는 것을 확인한 기록 (로그인 없이 열기 / 남의 패스키로 열기 / 이미 쓴 질문 재사용 / 패스키 삭제 뒤 로그인 — 네 가지 각각 성공·거절 요청 나란히)
   5. AI와 나 (아래 "AI와 내 판단 3줄"과 동일 내용)
   6. 아직 못 막은 것 (최소 하나, 구체적으로. "없다"만 적으면 통과하지 않음)
4. **짧은 확인 방법 4줄** — 어디로 가나요 / 세 단계 안에 무엇을 하나요 / 무엇이 보이면 통과인가요 / 안 될 때는 무엇이 보이나요
5. **AI와 내 판단 3줄** — AI에게 맡긴 일 / 내가 직접 판단한 일 / AI 말을 따르지 않은 일(없다면 왜 없었는지)

## 완주 체크리스트 (원문)

- [ ] 공개 영역과 비공개 영역을 화면에서 갈라 두었다.
- [ ] 패스키를 등록했고, 서버에 저장된 것이 공개키라는 것을 보였다.
- [ ] 매번 새 질문이 오고, 이미 쓴 질문은 다시 통하지 않는 것을 확인했다.
- [ ] 패스키를 두 개 등록해 하나를 지운 뒤에도 들어갔다.
- [ ] 남의 패스키로는 열리지 않는 것을 요청과 응답으로 남겼다.
- [ ] 제출물 어디에도 실제 개인정보와 비밀값이 없다.

## 증거 보관 기준

비공개 자리에 넣는 내용은 만들어 넣은 것으로 충분하다. 실제 연락처나 신분증 번호 같은 진짜 개인정보는 넣지 않는다. 심사하는 사람은 공개 소개 페이지까지만 보면 되고, 잠긴 자리는 열지 않는다.

---

## 카드 1 — 무엇을 잠글지 먼저 가른다

**하는 일**: 1번에서 만든 소개 페이지는 그대로 두고, 나만 보는 자리를 하나 새로 만든다.

**완료 목표**: 어디까지가 공개이고 어디부터가 비공개인지 화면에서 한눈에 보이게 나눈다.

**첫 행동**
1. 비공개 자리에 넣을 항목을 세 개 이상 정한다. 준비 중인 프로젝트 메모, 지원하려는 곳 목록, 스스로 쓰는 회고 같은 것이면 된다.
2. 공개 영역과 비공개 영역의 경계가 화면에서 바로 보이도록 배치한다.

**통과 기준**
- T08-C13 공개 영역과 비공개 영역이 화면에서 구분되어 보인다.
- T08-C14 비공개 영역에 들어 있는 항목이 세 개 이상이다.
- T08-C15 패스키로 들어가지 않은 상태에서는 비공개 영역의 내용이 화면에 보이지 않는다.
- T08-C16 패스키로 들어가지 않은 상태에서 비공개 자료를 서버에 직접 요청하면 거절된다.
- T08-C17 그 거절이 401 또는 403으로 이뤄진다.
- T08-C18 로그인하지 않은 상태로 받은 페이지의 소스 어디에도 비공개 내용이 들어 있지 않다.

**막히는 지점과 확인 순서**
- 화면에서는 안 보이는데 페이지 소스에는 비공개 내용이 그대로 들어 있음
  → 숨기기를 화면에서만 하지 않았는지 본다 → 비공개 내용을 서버가 로그인 뒤에만 내려 주는지 본다 → 로그인하지 않은 채 받은 응답 본문을 직접 열어 확인한다

**남길 것**: 공개 영역과 비공개 영역을 가른 화면, 그리고 로그인 없이 받은 응답 본문

---

## 카드 2 — 패스키를 등록한다

**하는 일**: 서버가 만든 일회용 질문을 브라우저에 넘기면, 기기가 열쇠 한 쌍을 만들고 공개키와 서명을 돌려준다.

**완료 목표**: 공개키만 서버에 저장되고 개인키는 기기를 떠나지 않는다는 것을 기록으로 보인다.

**첫 행동**
1. 등록용 질문을 서버에서 만들어 보내고, 확인할 때까지 그 값을 서버에 보관한다.
2. 돌아온 공개키를 저장하고, 그 패스키에 알아볼 수 있는 이름을 붙인다.

**통과 기준**
- T08-C19 서버가 등록용 질문(challenge)을 만들어 보내고, 그 값을 서버가 확인할 때까지 보관한다.
- T08-C20 등록 요청마다 질문 값이 서로 다르다는 기록이 제출문에 있다.
- T08-C21 등록이 끝나면 서버에 공개키가 저장된다.
- T08-C22 서버에 저장된 값이 제출문에 있고, 그 값이 공개키이며 비밀번호가 아니라는 설명이 함께 적혀 있다.
- T08-C23 개인키가 서버로 전송되지 않는다는 사실이, 등록 요청 본문을 적은 기록으로 확인된다.
- T08-C24 등록한 패스키에 사람이 알아볼 수 있는 이름이 붙는다.
- T08-C25 등록을 중간에 취소하면 화면에 안내가 나오고, 서버에 아무것도 저장되지 않는다.
- T08-C26 패스키를 저장한 곳이 어디인지(구글 비밀번호 관리자·기기 자체·보안 키 중 무엇인지)가 제출문에 적혀 있다.

**막히는 지점과 확인 순서**
- 패스키 창이 아예 뜨지 않음
  → 주소가 https인지 본다(개발 중에는 localhost만 예외 허용) → 브라우저와 기기가 패스키를 지원하는지 본다 → 이미 등록된 패스키가 있어 막힌 것은 아닌지 목록 화면에서 확인한다

**남길 것**: 등록 요청과 응답 기록, 서버에 저장된 공개키, 패스키 목록 화면

---

## 카드 3 — 패스키로 들어간다

**하는 일**: 로그인할 때도 서버가 매번 새 질문을 보내고, 기기가 개인키로 서명해 답한다.

**완료 목표**: 저장해 둔 공개키로 서명을 확인한 뒤에만 비공개 자리가 열리게 한다.

**첫 행동**
1. 로그인용 질문을 매번 새로 만들어 보내고, 서명을 공개키로 확인한다.
2. 이미 쓴 질문으로 다시 요청해 보고, 거절되는 것을 기록에 남긴다.

**통과 기준**
- T08-C27 로그인할 때도 서버가 매번 새 질문을 만들어 보낸다.
- T08-C28 로그인 요청마다 질문 값이 서로 다르다는 기록이 제출문에 있다.
- T08-C29 서버가 저장해 둔 공개키로 서명을 확인한 뒤에만 통과시킨다.
- T08-C30 서명 확인에 성공한 요청과 실패한 요청이 나란히 적혀 있다.
- T08-C31 이미 한 번 쓴 질문으로 다시 로그인하려는 요청과 그 거절 응답이 적혀 있다.
- T08-C32 로그인 뒤 무엇으로 사람을 알아보는지(세션·토큰 중 무엇인지)가 제출문에 적혀 있다.
- T08-C33 로그아웃한 뒤 같은 값으로 다시 요청했을 때의 거절 응답이 적혀 있다.
- T08-C34 적어 둔 기록에서 세션·토큰 값이 가려져 있다.
- T08-C35 제출물 어디에도 비밀번호를 입력하는 칸이 없다.

**막히는 지점과 확인 순서**
- 서명이 늘 통과함
  → 질문을 서버에 보관하지 않고 화면에서만 들고 있지 않은지 본다 → 서명 확인을 실제로 하고 있는지, 결과를 버리고 있지는 않은지 본다 → 일부러 틀린 서명을 보내 거절되는지 확인한다

**남길 것**: 성공한 로그인 요청과 실패한 요청, 이미 쓴 질문을 재사용한 요청과 거절 응답

---

## 카드 4 — 기기를 잃어버렸을 때

**하는 일**: 패스키는 기기에 매여 있다. 하나뿐이면 그 기기를 잃는 순간 못 들어간다.

**완료 목표**: 패스키를 두 개 등록하고, 하나를 지운 뒤에도 들어갈 수 있게 만든다.

**첫 행동**
1. 두 번째 패스키를 등록하고 목록 화면에서 둘 다 보이게 한다.
2. 하나를 지운 뒤 남은 하나로 들어가 보고, 지운 것으로는 안 되는지 확인한다.

**통과 기준**
- T08-C42 한 계정에 패스키가 두 개 등록되어 있다.
- T08-C43 등록된 패스키 목록을 화면에서 볼 수 있고, 각각의 이름과 등록한 날짜가 보인다.
- T08-C44 패스키 하나를 지운 뒤 남은 하나로 들어갈 수 있다.
- T08-C45 지운 패스키로는 더 이상 들어갈 수 없다는 기록이 적혀 있다.
- T08-C46 패스키가 하나도 남지 않았을 때 어떻게 되는지가 화면과 제출문에 적혀 있다.

**막히는 지점과 확인 순서**
- 같은 기기에 두 번째 패스키가 등록되지 않음
  → 이미 등록된 패스키를 제외하도록 서버가 알려 주고 있는지 본다 → 다른 기기나 다른 브라우저에서 등록해 본다 → 구글 비밀번호 관리자에 저장했다면 다른 기기에서도 보이는지 확인한다

**남길 것**: 패스키 두 개가 보이는 목록 화면, 하나를 지운 뒤의 로그인 성공과 실패 기록

---

## 카드 5 — 정말 안 열리는지 확인하고, 어떻게 붙였는지 적는다

**하는 일**: 막았다는 말 대신, 막힌 요청과 통과한 요청을 나란히 낸다.

**완료 목표**: 네 가지 확인을 마치고 설명서 여섯 항목으로 정리한다.

**첫 행동**
1. 패스키 없이 비공개 주소를 직접 열어 보고, 계정 두 개로 서로의 자료를 읽어 본다.
2. 설명서 여섯 항목을 나누어 적고, 아직 못 막은 것을 하나 이상 밝힌다.

**통과 기준**
- T08-C01 결과물 URL 필드에 HTTPS URL 한 개가 제출되어 있다.
- T08-C02 소스 URL 필드에 HTTPS URL 한 개가 제출되어 있다.
- T08-C03 제출한 모든 URL(결과물·소스)은 계정 생성·로그인·인증·초대·비밀번호·OAuth·CAPTCHA 없이 새 시크릿 창에서 열린다.
- T08-C10 결과물 주소의 첫 화면은 공개 소개 페이지이고, 심사하는 사람이 아무것도 등록하지 않아도 열린다.
- T08-C11 1번 과제에서 만든 소개 페이지에 이어 붙였고, 1번의 공개 내용이 그대로 남아 있다.
- T08-C12 제출물에 실제 개인정보가 없고, 넣은 내용이 만들어 넣은 것이라는 사실이 적혀 있다.
- T08-C36 패스키를 등록한 계정이 두 개이고, 각각에 서로 다른 비공개 내용이 들어 있다.
- T08-C37 한쪽 패스키로 들어가 다른 쪽의 비공개 자료를 읽으려는 요청과 그 거절 응답이 적혀 있다.
- T08-C38 반대 방향(다른 쪽에서 첫 쪽으로)의 요청도 똑같이 거절된 기록이 적혀 있다.
- T08-C39 거절 앞뒤로 반대편의 자료 건수가 같다는 기록이 적혀 있다.
- T08-C40 주소나 요청 본문에 다른 계정을 적어 보낸 요청과, 그래도 내 자료만 돌아온 응답이 적혀 있다.
- T08-C41 위 거절을 만들어 내는 소스의 위치가 제출문에 적혀 있다.
- T08-C47 인증 구현 설명서의 여섯 항목(① 무엇으로 붙였나, ② 왜 그걸 골랐나, ③ 어디를 어떻게 고쳤나, ④ 안 열리는 것을 확인한 기록, ⑤ AI와 나, ⑥ 아직 못 막은 것)이 각각 나뉘어 적혀 있다.
- T08-C48 ①에 직접 구현·라이브러리·인증 서비스 중 무엇을 썼는지와 그 이름이 적혀 있다.
- T08-C49 ③에 등록·로그인·로그아웃·비공개 자료 조회 네 흐름이 소스의 어디를 지나는지 적혀 있다.
- T08-C50 ④에 확인 네 가지(로그인 없이 열기, 남의 패스키로 열기, 이미 쓴 질문 재사용, 패스키 삭제 뒤 로그인)가 모두 있고, 각각 성공한 요청과 거절된 요청이 나란히 적혀 있다.
- T08-C51 ⑥에 아직 못 막은 것이 최소 하나 구체적으로 적혀 있다. 없다고만 적으면 통과하지 않는다.
- T08-C52 짧은 확인 방법에 ① 어디로 가나요, ② 세 단계 안에 무엇을 하나요, ③ 무엇이 보이면 통과인가요, ④ 안 될 때는 무엇이 보이나요가 각각 나뉘어 적혀 있다.
- T08-C53 제출문에 ① AI에게 맡긴 일, ② 내가 직접 판단한 일, ③ AI 제안을 따르지 않은 일(없다면 왜 없었는지)이 각각 나뉘어 적혀 있다.

**막히는 지점과 확인 순서**
- 아직 못 막은 것이 떠오르지 않음
  → 패스키를 지울 때 다른 사람이 지울 수는 없는지 본다 → 질문을 보관하는 시간이 너무 길지 않은지 본다 → 마지막 패스키까지 지웠을 때 계정이 어떻게 되는지 본다

**남길 것**: 확인 네 가지의 요청과 응답, 인증 구현 설명서 여섯 항목
