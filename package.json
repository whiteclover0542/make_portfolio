{
  "name": "whiteclover-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "WHITECLOVER portfolio (static) + passkey-locked private area (Vercel serverless functions)",
  "scripts": {
    "dev": "vercel dev",
    "dev:local": "node scripts/dev-server.mjs"
  },
  "dependencies": {
    "@simplewebauthn/server": "^13.1.1",
    "@upstash/redis": "^1.35.5"
  },
  "devDependencies": {
    "playwright": "^1.63.0"
  }
}
